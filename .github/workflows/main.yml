name: CI
on: [push]
  - PR 上述语句？

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: pip install -r requirements.txt
    - run: pvtest -q
    - 看红灯——是——不是——少了一步？

deploy:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v4
    - run: ./deploy.sh
    - 谁都能生产？
